import { Meta } from '@storybook/addon-docs';
import { Dropdown } from '../src';
import { Token } from "../src/Token";

<Meta title="MDX/D. Existing Token" component={Dropdown} />

## Custom Token For MoonbladeUI

<br/>
<strong>Type of token</strong>
<br/><br/>
<Token />

## Design Token Responsibilities

<p>Design tokens are all the values needed to construct and maintain a design system — spacing, color, typography, object styles, animation, etc. — represented as data. These can represent anything defined by design: a color as a RGB value, an opacity as a number, an animation ease as Bezier coordinates. - <a href='https://spectrum.adobe.com/page/design-tokens/' target='_blank'>https://spectrum.adobe.com/page/design-tokens/</a></p>

